{
  "name": "ad-generator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "build": "rm -rf dist && mkdir -p dist && cp -R public/. dist/",
    "pages:dev": "npm run build && wrangler pages dev dist --compatibility-date=2025-05-14",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "wrangler": "^4.90.1"
  }
}
